(function($){


   // ==== BEGINS PLUGGIN ====
   $.fn.techslider = function(data, callback){

        // ==== CACHE DOM ====
        var $techslider = $(this);
        var $tsContent = $techslider.find('.tech-slider-inner');
        var $tsNext = $techslider.find('button.TS-next-btn');
        var $tsPrev = $techslider.find('button.MS-prev-btn');
        var $imgsFirst = $msContent.find('.active');
   }


})(jQuery);